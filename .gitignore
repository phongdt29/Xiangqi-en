node_modules/
npm-debug.log*
.DS_Store
*.log
.env

# Local deploy packages (contain real secrets - see README "Deploy production")
/deploy/*.zip
/deploy/*.sql
# Allow documentation files
!/deploy/*.md

# Claude Code local settings (machine-specific, not shared)
.claude/settings.local.json
